<h1>Latest Blog Posts</h1>
<% @posts.each do |post| %>
    <div class="card-body">
        <h3><%= post.title %></h3>
        <hr>
        <p><%= post.body %></p>
        <%= link_to 'Read more', post_path(post), :class=> 'btn btn-secondary' %>
    </div>
<% end %>
